!!! 5
%html
  %head>
    %title Feedback
    = stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags
    %body
      %p.notice= notice
      %p.alert= alert

      %ul
        - if signed_in?
          %li= link_to "Submissions", submissions_path
          %li
            Hello, #{current_user.name}! Want to #{link_to "sign out", destroy_user_session_path, method: :delete}?
        - else
          -# %li= link_to "Sign up", new_user_registration_path
          %li= link_to "Sign in with Google", user_omniauth_authorize_path(:google_oauth2)

      = yield